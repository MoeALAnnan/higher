#!/bin/usr/python3
def max_integer(my_list=[]):
    if list is not None:
        if len(list) = 0:
            return (None)
        else:
            new_list = sorted(my_list)
            return (new_list[-1])
