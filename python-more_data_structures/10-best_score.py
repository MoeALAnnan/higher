#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary is None:
        return (None)
    for v in sorted(a_dictionary.values()):
        max_value = v
    for k in a_dictionary:
        new_k = k
    return (k)
